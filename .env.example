# Direct PostgreSQL connection string. Use the provider's pooled URL in production.
DATABASE_URL=

# Public origin used when generating shortened URLs.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Existing production rate limiting.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
RATE_LIMIT_HASH_SECRET=
